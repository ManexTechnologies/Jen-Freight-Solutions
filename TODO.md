# Navbar Centered Circled Tabs + Social Media Dropdown

1. ✅ Update `styles.css`:
   - Convert `.nav` to CSS grid (`1fr auto 1fr`) so tabs are truly centered
   - Style `.nav-links` as a centered glass pill container
   - Style each tab as a circled pill with an `.active` state
   - Add `.nav-right`, `.nav-socials`, `.socials-toggle`, `.socials-menu` styles
   - Add `.nav-socials-mobile` styles (mobile social icons inside hamburger)
   - Update the mobile media query (hide desktop socials, show socials in hamburger, wider panel)
2. ✅ Update `script.js`:
   - Add social dropdown toggle (open/close, outside click, Escape)
   - Add active page link highlighting
3. ✅ Update `index.html` nav (centered tabs + social dropdown + mobile socials)
4. ✅ Update `services.html` nav
5. ✅ Update `vehicles.html` nav
6. ✅ Update `about.html` nav
7. ✅ Update `contact.html` nav
8. ✅ Test in browser
